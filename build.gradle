buildscript {
    apply from: "dependencies.gradle"
    dependencies {
        classpath pluginAndroid
        classpath pluginKotlin
    }
    repositories {
        google()
        jcenter()
    }
}